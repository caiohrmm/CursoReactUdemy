import React from 'react'

const ChildrenProps = ( {children} ) => {
  return (
    <div>
        <h1>
            Testando children
        </h1>
        {children}
    </div>
  )
}

export default ChildrenProps